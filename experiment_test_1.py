import py5
import numpy as np
import random

class WeaklySelfAvoidingWalk:

    max_steps = 1000
    cols = 10
    rows = 10
    grid = [[0 for _ in range(cols)] for _ in range(rows)]

    def __init__(step_count, max_steps, grid, weakener, spacing, offset):
        print("hi")

    